namespace Farm
{
    public class Crop
    {
        // There's not anything actually here
        // This just describes something that we can flesh out later
        // The nice thing is that it's a type, distinct from all others,
        // so we can still write methods that will only work on a crop (even 
        // if they can't do anything with it yet).
    }
}